class Solution:
    def minimumCost(self, cost: List[int]) -> int:
        a=[]
        a=min(cost)
        if 2>=len(cost):
            final=cost[0]+cost[1]
            c=0
            d=0
        else:
            c=sum(cost)
            d=min(cost)
            final=c-d
        return (final)
